spring:
  datasource:
    # 기존 RDS (flagmysql) 설정은 주석 처리됨
     url: jdbc:mysql://flagmysql.cpakwuq24j8f.ap-northeast-2.rds.amazonaws.com:3306/jpashop?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
     username: root
     password: 12341234
     driver-class-name: com.mysql.cj.jdbc.Driver

    # localhost MySQL 설정 (활성화됨)
#    url: jdbc:mysql://localhost:3306/jpabook?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
#    username: root
#    password: 12341234!
#    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    open-in-view: true
    hibernate:
      ddl-auto: none # 개발 시 ddl-auto: update 사용 가능
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
      use-new-id-generator-mappings: false
    show-sql: true
    properties:
      hibernate.format_sql: true
      dialect: org.hibernate.dialect.MySQL8InnoDBDialect

logging:
  level:
    org.hibernate.SQL: debug