spring:
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:mysql://localhost:3306/jpashop?serverTimezone=Asia/Seoul&characterEncoding=UTF-8}
    username: ${SPRING_DATASOURCE_USERNAME:root}
    password: ${SPRING_DATASOURCE_PASSWORD:}
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    open-in-view: true
    hibernate:
      ddl-auto: none # 개발 시 ddl-auto: update 사용 가능
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
      use-new-id-generator-mappings: false
    show-sql: true
    properties:
      hibernate.format_sql: true
      dialect: org.hibernate.dialect.MySQL8InnoDBDialect

coolsms:
  api-key: ${COOLSMS_API_KEY:NCSFSAYCZAJQUT6H}
  api-secret: ${COOLSMS_API_SECRET:LHRGOUAJSLABKXFT6FL4T5C7F4LNNUJO}
  from: ${COOLSMS_FROM:01034770782}

logging:
  level:
    org.hibernate.SQL: debug